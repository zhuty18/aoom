---
import { cateName, linkStory } from "../consts"
const { postId } = Astro.props
const postCate = postId.split("/")[0]
---

<a class={`cate ${postCate}`} href={linkStory(postCate)}>{cateName(postCate)}</a
>
